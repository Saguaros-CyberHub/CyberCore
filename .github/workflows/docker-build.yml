name: CyberCore Docker Build Check
on:
    push:
        branches:
        - main
        - dev
        - 'feat/**'
    pull_request:
        branches:
        - main

jobs:
    docker-build:
        name: Build Docker image
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repo
          uses: actions/checkout@v4

        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v3

        - name: Build Docker image
          uses: docker/build-push-action@v6
          with:
                context: .
                file: ./Dockerfile
                push: false
                tags: cybercore:test