# Cloudbase-Init configuration for Proxmox.
#
# Copied over the MSI's default conf by install-cloudbase-init.ps1. The stock
# config targets OpenStack's metadata HTTP service, which does not exist on
# Proxmox -- Proxmox attaches cloud-init as an ISO on a CD-ROM, so the ConfigDrive
# service is the only one that can find it.

[DEFAULT]
# The username here must match winrm_username in variables.pkr.hcl. That account
# already exists in the template (Sysprep does not delete local accounts), so
# CreateUserPlugin finds it rather than making a new one, and
# SetUserPasswordPlugin then applies whatever the Proxmox Cloud-Init password
# field holds. Point this at a different name and the Cloud-Init password field
# will silently manage an account nobody logs in as.
username=cactus-user
groups=Administrators
inject_user_password=true
first_logon_behaviour=no

metadata_services=cloudbaseinit.metadata.services.configdrive.ConfigDriveService

plugins=cloudbaseinit.plugins.common.mtu.MTUPlugin,
        cloudbaseinit.plugins.common.sethostname.SetHostNamePlugin,
        cloudbaseinit.plugins.windows.createuser.CreateUserPlugin,
        cloudbaseinit.plugins.common.setuserpassword.SetUserPasswordPlugin,
        cloudbaseinit.plugins.common.sshpublickeys.SetUserSSHPublicKeysPlugin,
        cloudbaseinit.plugins.windows.extendvolumes.ExtendVolumesPlugin,
        cloudbaseinit.plugins.common.networkconfig.NetworkConfigPlugin,
        cloudbaseinit.plugins.common.userdata.UserDataPlugin,
        cloudbaseinit.plugins.common.localscripts.LocalScriptsPlugin

bsdtar_path=C:\Program Files\Cloudbase Solutions\Cloudbase-Init\bin\bsdtar.exe
mtools_path=C:\Program Files\Cloudbase Solutions\Cloudbase-Init\bin\
local_scripts_path=C:\Program Files\Cloudbase Solutions\Cloudbase-Init\LocalScripts\

logdir=C:\Program Files\Cloudbase Solutions\Cloudbase-Init\log\
logfile=cloudbase-init.log
default_log_levels=comtypes=INFO,suds=INFO,iso8601=WARN,requests=WARN
verbose=true
debug=true

# Proxmox reboots are the operator's business, not ours.
allow_reboot=false
stop_service_on_exit=true
check_latest_version=false

mtu_use_dhcp_config=true
ntp_use_dhcp_config=true

[config_drive]
# Proxmox presents the cloud-init drive as an ISO9660 CD-ROM on ide2.
types=iso,vfat
locations=cdrom,hdd,partition
